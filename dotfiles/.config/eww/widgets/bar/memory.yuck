(defwidget memory []
  (tooltip
    (label
      :text "RAM Usage"
    )
    (box
      :orientation "horizontal"
      :space-evenly false
      :spacing 10 
      (label
        :text "" 
      )
      (progress
        :orientation "horizontal"
        :class "progress-bar"
        :valign "center" 
        :value {EWW_RAM.used_mem_perc}
        :width 5
      )
      (label
        :text "${round(EWW_RAM.used_mem_perc, 0)}%"
      )
    )
  ) 
)

