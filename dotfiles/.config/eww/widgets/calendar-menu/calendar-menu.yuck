(include "./widgets/calendar-menu/usage-bars/battery.yuck")
(include "./widgets/calendar-menu/usage-bars/cpu.yuck")
(include "./widgets/calendar-menu/usage-bars/disk.yuck")
(include "./widgets/calendar-menu/usage-bars/memory.yuck")
(include "./widgets/calendar-menu/power-buttons/lock.yuck")
(include "./widgets/calendar-menu/power-buttons/reboot.yuck")
(include "./widgets/calendar-menu/power-buttons/power-off.yuck")

(
    defwindow calendar-menu
    :monitor 0
    :exclusive false
    :stacking "overlay"
    :geometry (geometry
      :anchor "top center"
      :y -50
    )
    (eventbox
      :onhoverlost "~/.config/eww/scripts/toggle-calendar-menu.sh"
      (box
        :orientation "vertical"
        :class "container"
        :space-evenly false
        :spacing 10
        (calendar)
        (battery)
        (cpu)
        (disk)
        (memory)
        (box
          :orientation "horizontal"
          :space-evenly false
          :spacing 10
          (lock)
          (reboot)
          (power-off)
        )
      )
    )
)

